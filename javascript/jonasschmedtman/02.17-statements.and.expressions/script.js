// Function Statement, proruces action
function somFunction(parameter) {
	// code
}

// Functions expressions, produces an outcome, a value
var someFunction = function(parameter) {
	//code
}

// Expressions
3+4;
var x + 3;

//statements
if (x===5) {
	// do something
}